import actions from './actions';
import mutations from './mutations';

const state = {
    prisonAreas: { content: [] },
    prisonArea: {},
    prisonAreasJail: { content: [] }
};

export default {
    state,
    actions,
    mutations
};
