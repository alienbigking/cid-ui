import actions from './actions';
import mutations from './mutations';

const state = {
    prisonAreas: { content: [] },
    prisonArea: {}
};

export default {
    state,
    actions,
    mutations
};
